### Build the website
1. `$ cd photo` - go to the project directory
2. `$ npm install` - install all npm dependencies

### Resize Images
Start with exporting all fulls into the base 'images' directory and running the 'resize.js' from the root of the project.

`node resize.js`

